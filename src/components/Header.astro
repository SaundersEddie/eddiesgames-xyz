<header class="header">
  <div class="container header-inner">
    <a href="/" class="logo">EDDIES GAMES</a>

    <nav class="nav">
      <a href="/about">About</a>
      <a href="/updates">Updates</a>
    </nav>
  </div>
</header>

<style>
.header {
  border-bottom: 1px solid var(--line);
  background: rgba(10, 14, 18, 0.8);
  backdrop-filter: blur(8px);
}

.header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 60px;
}

.logo {
  letter-spacing: 0.2em;
  font-weight: bold;
}

.nav {
  display: flex;
  gap: 24px;
}

.nav a {
  font-size: 0.9rem;
  color: var(--muted);
  transition: color 0.2s ease;
}

.nav a:hover {
  color: var(--text);
}
</style>